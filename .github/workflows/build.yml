name: Java CI with Gradle

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4

    - name: Unzip project archive
      run: |
        unzip *.zip
        echo "=== СТРУКТУРА ПАПОК ПОСЛЕ РАСПАКОВКИ ==="
        find . -maxdepth 3 -not -path '*/.*'

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: 'gradle'

    - name: Find, Fix and Build with Gradle
      run: |
        # Ищем, в какой папке лежит gradlew
        GRADLEW_PATH=$(find . -name "gradlew" -print -quit)
        
        if [ -z "$GRADLEW_PATH" ]; then
          echo "ОШИБКА: Файл gradlew не найден ни в одной папке архива!"
          exit 1
        fi
        
        PROJECT_DIR=$(dirname "$GRADLEW_PATH")
        echo "Проект найден в папке: $PROJECT_DIR"
        
        # АВТО-ПОЧИНКА: Удаляем устаревшую строчку bootClasspath, из-за которой падал Gradle
        if [ -f "$PROJECT_DIR/build.gradle" ]; then
          echo "Удаляем bootClasspath из build.gradle..."
          sed -i '/bootClasspath/d' "$PROJECT_DIR/build.gradle"
        fi
        
        # Даем права и собираем
        chmod +x "$PROJECT_DIR/gradlew"
        cd "$PROJECT_DIR"
        ./gradlew build --no-daemon

    - name: Upload Mod Artifact
      uses: actions/upload-artifact@v4
      with:
        name: Compiled-Mod
        path: "**/build/libs/*.jar"
        
